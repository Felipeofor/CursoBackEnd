class Producto{
    constructor(title, price, thumbnail, mascotas){
        this.title = title || "";
        this.price = price || "";
        this.thumbnail = thumbnail || ""
    }
}

export default Producto;
